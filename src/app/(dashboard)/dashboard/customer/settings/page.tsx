
import SettingsPage from '@/components/pages/dashboard/settings/SettingsPage';
import React from 'react';

const page = () => {
    return (
      <div>
        <SettingsPage />
      </div>
    );
};

export default page;